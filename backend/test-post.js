const fetch = require('node-fetch'); // wait node 18+ has fetch natively

async function run() {
  // 1. Get a token or mock it
  // Actually we need a valid token to bypass `protect` and `authorize('admin', 'teacher')`
  // Can we generate a token directly?
}
run();
